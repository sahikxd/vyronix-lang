class Person:
    unit init(self, name, city):
        set self.name = name
        set self.city = city
    
    unit info(self):
        echo self.name + " lives in " + self.city

let p = new Person("Bob", "Dhaka")
p.info()
